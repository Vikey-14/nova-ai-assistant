# macOS requirements.txt

# --- macOS bridges for tray (required by pystray) ---
pyobjc-core
pyobjc-framework-Cocoa
pyobjc-framework-Quartz

# --- Tray & UI ---
pystray
Pillow

# --- Speech / Audio ---
pyttsx3==2.99
SpeechRecognition==3.14.3
PyAudio==0.2.14          # needs PortAudio on the build machine (brew install portaudio)
gTTS==2.5.4
playsound==1.3.0
edge-tts>=6.1.0
aiohttp>=3.9             # edge-tts depends on this

# --- Streaming ASR (offline, one-shot) ---
vosk>=0.3.45             # REQUIRED: you ship the models; this is the Python API
webrtcvad>=2.0.10        # OPTIONAL but recommended (snappier barge-in)

# --- NLP / Web / Utilities ---
langdetect
requests>=2.31.0
python-dotenv
wikipedia
beautifulsoup4
dateparser
python-dateutil
holidays

# --- Math / Plotting ---
numpy>=2.0
sympy
matplotlib

# --- System utils ---
psutil
platformdirs
